// const resolveEntryForPkg = (p) =>
//   path.resolve(
//     fileURLToPath(import.meta.url),
//     `../../packages/${p}/src/index.ts`
//   )

// export const entries = {
//   typure: resolveEntryForPkg('typure-core'),
// }
